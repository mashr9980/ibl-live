// Vitest stub for Next.js `server-only`. Throws in dev/prod to mark code as
// server-only; we override the import in vitest.config.ts so this no-op
// module is used during tests instead.
export {};
