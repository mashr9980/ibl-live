// Originally read the source package's own package.json to derive this; now
// that the hooks are vendored into the app, the SDK version is pinned in the
// app's package.json — keep this in sync with
// the `@heygen/liveavatar-web-sdk` dependency version there.
export const SDK_VERSION = '0.0.18';
